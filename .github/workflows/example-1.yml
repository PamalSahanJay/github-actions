name: my workflow
on:
  push:
    branches:
      - main
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
          - name: Run a script
            run: echo "Hello, world!"
          - name: list files
            run: ls -la
          - name: read file
            run: cat ex.txt